# pytest.ini
[pytest]
markers =
    integration: marks tests as integration tests (requiring external dependencies like GWB)

addopts = --strict-markers -s -v

testpaths = 
    tests

python_files =
    test_*.py